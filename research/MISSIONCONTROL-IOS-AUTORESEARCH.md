# MissionControl iOS Autoresearch Plan

## Baseline
- **Current compile error count: 0**
- Error types: None — build succeeds cleanly
- Affected files: None

## Metric
- **Primary metric:** Swift compile error count (integer, lower is better)
- **Measurement:** `xcodebuild` exit code + `grep -c "error:"` count
- **Goal:** 0 errors (already achieved)

## Verification Command

```bash
cd /Volumes/Data/openclaw/workspace/projects/iOS/MissionControl && \
xcodebuild -project MissionControl.xcodeproj -scheme MissionControl \
  -destination 'platform=iOS Simulator,id=6586C72E-E7B4-4609-98E2-27DDE9259AF0' \
  build 2>&1 | grep -c "error:"
```

**Expected output when clean:** `0` (exit code 0)
**Expected output when broken:** N > 0 (exit code may be non-zero)

## Scope

### Files That CAN Be Modified (all Swift source)
```
MissionControl/App/
  MissionControlApp.swift
  ContentView.swift

MissionControl/Models/
  Task.swift
  MCAgent.swift
  MCStep.swift
  ActivityEvent.swift

MissionControl/Views/Tasks/
  TaskListView.swift
  TaskFilterBar.swift
  TaskDetailView.swift

MissionControl/Views/Activity/
  ActivityFeedView.swift

MissionControl/Views/Agents/
  AgentsView.swift

MissionControl/Views/Login/
  LoginView.swift

MissionControl/Components/
  DesignSystem.swift

MissionControl/Services/
  APIClient.swift
  SecureStorage.swift

MissionControlWidget/
  MissionControlWidget.swift
  MissionControlWidgetBundle.swift

MissionControlTests/
  MissionControlTests.swift
```

### Files That Are READ-ONLY (generated/config)
```
MissionControl.xcodeproj/       (Xcode-managed, do not edit manually)
*.xcassets/                     (asset catalogs)
*.xcodeproj/project.pbxproj      (auto-generated by Xcode)
Info.plist (generated)          (managed by Xcode)
```

## Success Criteria

- `xcodebuild ... build 2>&1 | grep -c "error:"` returns `0`
- `xcodebuild ... build 2>&1` ends with `** BUILD SUCCEEDED **`
- All 18 Swift files compile without type errors

## First Iteration Priority

Since the project already builds with **0 compile errors**, the loop is at its goal state.

If the loop is re-purposed for a secondary metric (e.g., warning reduction), the priority order would be:

1. **APIClient.swift** — largest service file (261 lines), handles all network/JSON with custom date decoding, most surface area for runtime/decoding bugs
2. **ActivityFeedView.swift** (232 lines) — complex async view with event streaming
3. **AgentsView.swift** (222 lines) — agent management UI, potential state issues
4. **DesignSystem.swift** (285 lines) — shared design tokens and view modifiers
5. **TaskListView.swift** (179 lines) — list/filter/view model integration

## Notes

- Build uses scheme `MissionControl` (not the widget extension scheme)
- Simulator: iPhone 16 Pro (id: `6586C72E-E7B4-4609-98E2-27DDE9259AF0`, iOS 18.1+)
- Target: iOS 17.0+
- There is 1 non-blocking warning: `appintentsmetadataprocessor` — "Metadata extraction skipped. No AppIntents.framework dependency found." This is informational and does not affect the error count metric.
